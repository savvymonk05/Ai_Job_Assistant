/**
 * Security Layer — JWT authentication components.
 *
 * Contains:
 * - JWT token utility (generate, validate, extract claims)
 * - JWT authentication filter (intercepts every request)
 * - Custom UserDetailsService implementation
 * - Authentication entry point for unauthorized access
 */
package com.himanshu.aijobassist.security;
