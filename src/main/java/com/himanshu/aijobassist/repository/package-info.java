/**
 * Repository Layer — Data access (Database operations).
 *
 * Repositories extend Spring Data JPA interfaces.
 * They provide CRUD operations and custom queries for entities.
 * No business logic should exist here — only data fetching/storing.
 */
package com.himanshu.aijobassist.repository;
