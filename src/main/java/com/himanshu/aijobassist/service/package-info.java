/**
 * Service Layer — Business logic.
 *
 * Services contain the core logic of the application.
 * They are called by controllers and can call repositories.
 * This layer is reusable across different interfaces (REST, GraphQL, CLI, etc.)
 */
package com.himanshu.aijobassist.service;
