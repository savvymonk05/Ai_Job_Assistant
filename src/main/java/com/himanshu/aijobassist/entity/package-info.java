/**
 * Entity Layer — JPA entities that map to database tables.
 *
 * Each entity class represents a table in PostgreSQL.
 * Use JPA annotations (@Entity, @Table, @Column) to define the mapping.
 * Entities should NOT be exposed directly in API responses — use DTOs instead.
 */
package com.himanshu.aijobassist.entity;
